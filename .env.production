# Backend URL used by `npm run build`. Put your deployed Worker URL here, e.g.
# https://amalgramme-community.<you>.workers.dev  (no trailing slash needed).
# Prefer .env.production.local (gitignored via *.local) if you'd rather keep it out of git.
VITE_COMMUNITY_API=https://amalgramme-community.amalgramme.workers.dev
